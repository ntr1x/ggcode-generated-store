server:
  port: 8080
  forward-headers-strategy: framework
  servlet:
    context-path: /api/service_profile

spring:
  application:
    name: service_profile
  config:
    import:
      - classpath:shared-datasource.yaml
      - classpath:service_profile.yaml

springdoc:
  api:
    title: Store
    description: Store Description
    version: 1
