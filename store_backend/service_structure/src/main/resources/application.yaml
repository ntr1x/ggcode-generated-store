server:
  port: 8080
  forward-headers-strategy: framework
  servlet:
    context-path: /api/service_structure

spring:
  application:
    name: service_structure
  config:
    import:
      - classpath:shared-datasource.yaml
      - classpath:service_structure.yaml

springdoc:
  api:
    title: Store
    description: Store Description
    version: 1
